apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "watertrack.fullname" . }}-test-connection"
  labels:
    {{- include "watertrack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "watertrack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
